{
  "name": "zhu-mancheng-legal-website-demo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/sync-events.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "release": "npm run build && node scripts/package-release.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
